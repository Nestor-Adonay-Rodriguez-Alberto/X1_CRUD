@model Entidades.Ciudad

@{
    ViewData["Title"] = "Details";
}

<h1>Detales</h1>
<br />

<div>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayName("CIUDAD")
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Nombre)
        </dd>
    </dl>
</div>


<div>
    @Html.ActionLink("Editar", "Edit", new {  id = Model.IdCiudad  }) |
    <a asp-action="Index">Cancelar</a>
</div>
